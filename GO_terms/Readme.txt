Notes on use:

The python script was written by Stefan Siebert. Use this on the GO prediction from the PANNZER2 server.

You'll need to change the file name at the end before running the script.

Also edit the species names out (e.g. Agalma_elegans@) and add a header (optional)
